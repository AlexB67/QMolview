#ifndef APPGLOBALS_H
#define APPGLOBALS_H
#include <QString>

namespace SystemSettings
{
// A hack because Qt doesn't have a setting for this
    inline QString systemstyle;
}

#endif // APPGLOBALS_H
